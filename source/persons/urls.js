/**
 * @fileoverview URLs for the persons context.
 */


// constants
const baseUrl = '/users';


/**
 * @exports
 */
module.exports = {
  media: '/media/persons',
  index: baseUrl + '/',
  model: baseUrl + '/:slug',
  register: baseUrl + '/register',
};
